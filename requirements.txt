# Python deps for src/ + Streamlit app (single file at repo root — Streamlit Cloud also finds this
# when no dependency file exists under app/).
#
# en_core_web_sm wheel: NER on Cloud without `spacy download`. Local alternative:
#   python -m spacy download en_core_web_sm

joblib>=1.4.0
numpy>=2.0.0
pandas>=2.0.0
scikit-learn>=1.5.0
sentence-transformers>=5.0.0
# transformers imports optional vision modules; missing torchvision breaks import on Cloud
torchvision>=0.15.0
spacy>=3.7.0,<4.0.0
dateparser>=1.2.0
streamlit>=1.40.0

# en_core_web_sm (3.8.x) for NER — installable via pip on Cloud without `spacy download`
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
